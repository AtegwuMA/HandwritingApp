# Export (Phase 3/5)

ONNX conversion + INT8 quantization scripts for the HTR model
(`ml/htr_training/`) and the correction model (`ml/correction_training/`),
via `optimum`/`onnxruntime-tools`. Output goes to
`mobile/assets/models/` for bundling into the Flutter app.
