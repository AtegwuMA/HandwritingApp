# Datasets

Not checked into git (see root `.gitignore`). Expected layout once fetched:

```
datasets/
  iam/
  rimes/
  cvl/
  self_collected/
```

- IAM Handwriting Database, RIMES, CVL: standard HTR training/eval sets
  (Phase 3).
- `self_collected/`: any samples gathered for style diversity, plus the
  Phase 1 benchmark manifest (`ml/eval/`) sample images.
